[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bbp-common"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["bbp-schemas>=0.1.0", "bbp-scope-engine>=0.1.0"]
